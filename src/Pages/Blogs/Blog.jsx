import React from 'react';

const Blog = ({blog}) => {
    const {title, description, image, category, like, comment, author, authorTitle} = blog
    return (
        <div>
            <h1>new blog</h1>
        </div>
    );
};

export default Blog;