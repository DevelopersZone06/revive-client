import { Outlet } from "react-router-dom/dist";


const Root = () => {
    return (
        <div>
            
            <Outlet>

            </Outlet>
            
        </div>
    );
};

export default Root;